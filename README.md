# ARP Spoofing Script

This Python script performs an ARP spoofing on a target machine within a network, modifying ARP tables to redirect packets through a specified machine. **Use this tool responsibly and only on networks where you have permission.**

## Requirements

- Python 3.6+
- Scapy library for packet manipulation

## RUN

1. Clone this repository:
   ```bash
   git clone https://github.com/w0rmr/42_dd05
   cd 42_dd05
   pip install -r requirements.txt
   python start.py

